n: 10

func fib(n)
    func add()
        return fib(n) + fib(n - 1)
    ifnot n < 2
        return add()
    return 1

if n > 1_234_567_890
    write(fib(10))
else
    write(false)

func print_10()
    if true
        write(+1_234._5)

x: print_10
y: fib

[]
[0]
[ panty, stocking, garterbelt(chuck) ]
[ [zergling, hydralisk], [marine, firebat], [zealot, dragoon] ]
[0] ++ []
z ++ z ++ z

[].size()
x.empty()
xx().first()
([] ++ []).push_back(0)
x.xx().first()

[] |? $i = 0 |: !$ * $k

s: 'sizukani tiriyuku' + "the quick brown fox jumps over a lazy dog."
sk: "a quote\" and something \\else in\t the\n string"
st: 'nomico"misato' + "nomico'misato"

import emirin
import kagami, hiiragi

emirin.katou: kagami.hiiragi

x: {
    _1: 2,
    _3: 4,
    _5: 7
}
y: {} # this is comment

# this is comment
  # this is comment with leading space

triple_quoted: '''<input type='text' value=''>''' + '''\\\'''' + '''
this very long text will
take at least 3 lines
as it said
''' + '
single quoted
'
line_test: 0

log('I think ' + typeof 0 + ' is the type of 0.')

bitwise: x + ~-y * z
bitwiseX: x < ~y << z
bitwiseY: x >>> ~y & z

try
    f()
catch
    g($exception)
