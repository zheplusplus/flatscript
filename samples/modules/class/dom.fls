class Animal
    func sound()
        return 'Animal doesnt have a sound'

class Cat: Animal
    ctor(name) super()
       this.name: name

    func sound()
        return this.name + ' meow'

export Animal: Animal
export Cat: Cat
